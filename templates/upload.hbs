<!DOCTYPE html>
<html>
<head>
  <title>Upload Images</title>
  <style>
      body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      background:transparent;
      min-height: 100vh;
      background: url('https://propertybranding.co.uk/wp-content/uploads/2022/11/Brick-Background.jpg') no-repeat;
      background-position: top;
      background-size: cover;
    }
   
    form {
      width: 400px;
      background-color: #fff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      align-items: center;
      background:transparent;
      backdrop-filter: blur(20px);
      box-shadow: 0 0 30px rgba(0,0,0,.5);
    }

    .option {
      width: 250px;
      text-align: center;
      margin: 10px 20px;
      box-shadow: 0px 0px 10px #ddd;
      padding: 20px;
      transition: all 0.3s ease-in-out;
    }
    
    .option:hover {
      transform: translateY(-10px);
    }
    
    h2 {
      margin-top: 0;
    }
    
    p {
      margin: 10px 0;
    }

      .btn {
      width: 100%;
      height: 55px;
      background: #162938;
      border: none;
      outline: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1em;
      color: #fff;
      font-weight: 500;
      margin-top: auto;
    }

   h1 {
  text-align: center;
  margin-top: 0;
  margin-bottom: 20px;
}
  </style>
</head>
<body>
  <form action="/upload" method="POST" enctype="multipart/form-data">
    <label for="traditional">Upload here:</label>
    <input type="file" name="file" id="traditional"><br>
    <label for="category">Category:</label>
    <input type="text" name="category" id="category"><br>
    <input type="submit" value="Upload">
  </form>
 
</body>
</html>